<!DOCTYPE html>
<html>
    <head>
        <title>Weather in <%= weather.name %></title>
        <link rel="stylesheet" type="text/css" href="/main.css" />
        <link rel="stylesheet" type="text/css" href="/weather.css" />
    </head>
    <body>
        <div class="weather-container">
            <h1>Weather Result</h1>
            <div class="weather-header"><%= weather.name %>, <%= weather.sys.country %></div>

            <% if (weather.weather && weather.weather.length > 0) { %>
            <img
                src="http://openweathermap.org/img/w/<%= weather.weather[0].icon %>.png"
                alt="Weather icon"
                class="weather-icon"
            />
            <div style="text-transform: capitalize; font-weight: bold"><%= weather.weather[0].description %></div>
            <% } %>

            <div class="weather-temp"><%= Math.round(weather.main.temp) %>°C</div>

            <div class="weather-details">
                <div class="weather-detail-item"><strong>Humidity:</strong> <%= weather.main.humidity %>%</div>
                <div class="weather-detail-item"><strong>Wind Speed:</strong> <%= weather.wind.speed %> m/s</div>
                <div class="weather-detail-item"><strong>Pressure:</strong> <%= weather.main.pressure %> hPa</div>
                <div class="weather-detail-item">
                    <strong>Feels Like:</strong> <%= Math.round(weather.main.feels_like) %>°C
                </div>
            </div>

            <a href="/weather" class="back-link">Check Another City</a>

            <div style="margin-top: 15px">
                <a href="/" class="back-link" style="font-size: 0.9rem">Home</a>
            </div>
        </div>
    </body>
</html>
